function computerStore(arr) {
    let priceNoTax = 0;

    for (let i = 0; i < arr.length; i++) {
       let command = arr[i]
       if (command != "regular" && command != "special"){

       }else{
        ...arguments.
       }
        console.log(command);
    }
}
computerStore([
    '1023',
    '15',
    '-20',
    '-5.50',
    '450',
    '20',
    '17.66',
    '19.30', 'regular'
]);